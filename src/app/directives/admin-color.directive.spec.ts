import { UsernameDirective } from './username.directive';

describe('AdminColorDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameDirective();
    expect(directive).toBeTruthy();
  });
});
